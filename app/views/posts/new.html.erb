<h3>Make a new post</h3>

<%= form_for :posts, url: posts_path do |f| %>
	<%= f.label :title %><br>
	<%= f.text_field :title %><br>
	<%= f.label :body %><br>
	<%= f.text_area :body %><br>
	<%= f.submit %>
<% end %>